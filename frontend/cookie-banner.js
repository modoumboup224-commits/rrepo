(function () {
    // Minimal stub to prevent 404/mime-type errors from breaking the page.
    // If your real cookie banner exists elsewhere, replace this file.
    try {
        console.log('[cookie-banner] loaded');
    } catch (e) { }
})();

