// Configuration pour l'API
const API_BASE_URL = window.location.hostname === 'localhost' ? 'http://localhost:6300' : 'https://rrrrepo.onrender.com'; // URL locale ou Render du backend

// Exemple d'utilisation :
// const response = await fetch(`${API_BASE_URL}/api/products`);
